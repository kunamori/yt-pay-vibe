# Google Drive Configuration
# For GitHub Actions: Add these as repository secrets in Settings > Secrets and variables > Actions
# For local development: Create a .env.local file with these values
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here
GOOGLE_SERVICE_ACCOUNT_EMAIL=your_service_account_email@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Admin Credentials
# For production, use bcrypt hashed password: bcrypt.hashSync('your_password', 10)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password_here

# Session Secret (generate a random string - at least 32 characters)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_session_secret_here

# Optional: Base path for deployment (used for GitHub Pages subdirectory deployment)
NEXT_PUBLIC_BASE_PATH=

